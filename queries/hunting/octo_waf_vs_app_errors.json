{
  "title": "Octo WAF Detections vs App 5xx",
  "description": "Joins OCI WAF detection events with Shop/CRM 5xx responses using the shared X-Request-Id header. Used to build the Security Posture dashboard and to feed the waf-tighten-suggest playbook.",
  "query": "('Log Source' = 'SOC Application Logs' or 'Log Source' = 'SOC WAF Security Logs') and ('WAF Action' != null or 'HTTP Status Code' >= 500) | eval haswaf = if('WAF Action' != null, 1, 0) | eval is5xx = if('HTTP Status Code' >= 500, 1, 0) | stats count as events, sum(haswaf) as waf, sum(is5xx) as app5xx by 'Client IP', 'URL Path' | where waf > 0 and app5xx > 0 | sort -events",
  "type": "hunting",
  "category": "initial_access",
  "mitre_attack": {
    "tactics": [
      "initial_access"
    ],
    "techniques": [
      "T1190"
    ]
  },
  "data_source": "octo-waf, octo-shop-v2, octo-crm-v2",
  "frequency": "hourly",
  "notes": "Non-zero output is strong evidence of probing that bypassed or stressed the WAF.",
  "logsource": {
    "product": "multi",
    "service": "web"
  }
}