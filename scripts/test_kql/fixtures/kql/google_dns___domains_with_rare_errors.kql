('Log Source' = 'SOC WAF Security Logs' or 'Log Source' = 'SOC Web Application Logs' or 'Log Source' = 'SOC Network Firewall Logs') and (Status != 'NOERROR') | stats count as count_ by Status, 'Query Name' | sort count_ | sort -count_ | head 10